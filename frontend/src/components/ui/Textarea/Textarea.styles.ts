import { cva } from "cva";

export const textareaStyles = cva("flex flex-col gap-2");

export const labelStyles = cva("font-semibold");

export const textareaElementStyles = cva(
  "border-b-2 border-neutral-300 p-2 bg-white"
);
